<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="home"
            type="com.wonders.xlab.patient.module.home.adapter.bean.HomeItemBean"/>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                  android:layout_width="match_parent"
                  android:layout_height="?attr/listPreferredItemHeightLarge"
                  android:layout_margin="@dimen/item_interval_small"
                  android:background="@drawable/shape_rectangle_primary_border_translate_bg"
                  android:gravity="center"
                  android:orientation="horizontal">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="centerCrop"
            android:src="@{home.drawableResId}"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/item_interval_small"
            android:text="@{home.title}"
            android:textColor="@color/text_color_primary_black"
            android:textSize="@dimen/text_size_large"/>

    </LinearLayout>
</layout>